pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{ts,tsx,js,jsx,json}"
      exclude: "public/data/**"
      run: pnpm biome check --write --staged {staged_files}
      stage_fixed: true
    typecheck:
      run: pnpm tsc --noEmit
