# Tasks: Language Chips Sort + Fallback Colors + Hero Background

## 1. Sort function

- [ ] 1.1 Add `sortByFrequency(userLangs, allLangs)` to `ranking.ts` with tests
- [ ] 1.2 Use in `RankingFilter.svelte` to sort `user.languages.slice(0, 3)` display

## 2. Fallback colors

- [ ] 2.1 Expand `LANG_COLORS` in `language-colors.ts` with more languages (Shell, CSS, Jupyter Notebook, Vim Script, C, Tcl, HTML, etc.)
- [ ] 2.2 Improve fallback color from `#555` to `#6b7280` in RankingFilter and ProfilePage

## 3. Hero background

- [ ] 3.1 Add light mode hero background (currently only dark has night-sky.png)
